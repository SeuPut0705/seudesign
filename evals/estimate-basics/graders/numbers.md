Score 1.0 only if ALL of the following hold, otherwise score proportionally lower:

1. The answer computes average read RPS from 5M/day (should be roughly 55-60 RPS) and mentions a peak multiplier.
2. The answer computes yearly raw storage from 100k uploads/day × 20MB × 365 (roughly 730TB/year before replication) and applies the 3× replication factor (~2.2PB).
3. The arithmetic shown is order-of-magnitude correct (no results off by 10× or more).
4. The answer interprets the numbers for design decisions (e.g. object storage needed, CDN for reads) instead of only listing figures.
